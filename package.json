{
  "name": "vite-project",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "deploy": "rm -rf docs && yarn build && cp -r dist docs"
  },
  "dependencies": {
    "@babylonjs/core": "^5.0.0-alpha.56",
    "@babylonjs/loaders": "^5.0.0-alpha.56",
    "@babylonjs/materials": "^5.0.0-alpha.56",
    "cannon": "^0.6.2"
  },
  "devDependencies": {
    "@types/cannon": "^0.1.7",
    "typescript": "^4.3.2",
    "vite": "^2.6.4"
  }
}
